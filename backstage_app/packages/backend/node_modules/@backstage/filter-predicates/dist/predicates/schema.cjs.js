'use strict';

var errors = require('@backstage/errors');
var v3 = require('zod-validation-error/v3');
var zodV3 = require('zod/v3');

function _interopNamespaceCompat(e) {
  if (e && typeof e === 'object' && 'default' in e) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var zodV3__namespace = /*#__PURE__*/_interopNamespaceCompat(zodV3);

function createZodV3FilterPredicateSchema(z) {
  const primitiveSchema = z.union([
    z.string(),
    z.number(),
    z.boolean()
  ]);
  let valuePredicateSchema;
  const expressionSchema = z.lazy(
    () => z.union([
      z.record(z.string().regex(/^(?!\$).*$/), valuePredicateSchema),
      z.record(z.string().regex(/^\$/), z.never())
    ])
  );
  const predicateSchema = z.lazy(
    () => z.union([
      expressionSchema,
      primitiveSchema,
      z.object({ $all: z.array(predicateSchema) }),
      z.object({ $any: z.array(predicateSchema) }),
      z.object({ $not: predicateSchema })
    ])
  );
  valuePredicateSchema = z.union([
    primitiveSchema,
    z.object({ $exists: z.boolean() }),
    z.object({ $in: z.array(primitiveSchema) }),
    z.object({ $contains: predicateSchema }),
    z.object({ $hasPrefix: z.string() })
  ]);
  return predicateSchema;
}
function parseFilterPredicate(value) {
  const schema = createZodV3FilterPredicateSchema(zodV3__namespace.z);
  const result = schema.safeParse(value);
  if (!result.success) {
    throw new errors.InputError(
      `Invalid filter predicate: ${v3.fromZodError(result.error)}`
    );
  }
  return result.data;
}

exports.createZodV3FilterPredicateSchema = createZodV3FilterPredicateSchema;
exports.parseFilterPredicate = parseFilterPredicate;
//# sourceMappingURL=schema.cjs.js.map
