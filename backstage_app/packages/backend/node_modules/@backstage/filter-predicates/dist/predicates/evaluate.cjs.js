'use strict';

var getJsonValueAtPath = require('./getJsonValueAtPath.cjs.js');

function evaluateFilterPredicate(predicate, value) {
  if (typeof predicate !== "object" || predicate === null || Array.isArray(predicate)) {
    return valuesAreEqual(value, predicate);
  }
  if ("$all" in predicate) {
    return predicate.$all.every((f) => evaluateFilterPredicate(f, value));
  }
  if ("$any" in predicate) {
    return predicate.$any.some((f) => evaluateFilterPredicate(f, value));
  }
  if ("$not" in predicate) {
    return !evaluateFilterPredicate(predicate.$not, value);
  }
  for (const filterKey in predicate) {
    if (!Object.hasOwn(predicate, filterKey)) {
      continue;
    }
    if (filterKey.startsWith("$")) {
      return false;
    }
    if (!evaluateFilterPredicateValue(
      predicate[filterKey],
      getJsonValueAtPath.getJsonValueAtPath(value, filterKey)
    )) {
      return false;
    }
  }
  return true;
}
function filterPredicateToFilterFunction(predicate) {
  return (value) => evaluateFilterPredicate(predicate, value);
}
function evaluateFilterPredicateValue(filter, value) {
  if (typeof filter !== "object" || filter === null || Array.isArray(filter)) {
    return valuesAreEqual(value, filter);
  }
  if ("$contains" in filter) {
    if (!Array.isArray(value)) {
      return false;
    }
    return value.some((v) => evaluateFilterPredicate(filter.$contains, v));
  }
  if ("$in" in filter) {
    return filter.$in.some((search) => valuesAreEqual(value, search));
  }
  if ("$exists" in filter) {
    if (filter.$exists === true) {
      return value !== void 0;
    }
    return value === void 0;
  }
  if ("$hasPrefix" in filter) {
    if (typeof value !== "string") {
      return false;
    }
    return value.toLocaleUpperCase("en-US").startsWith(filter.$hasPrefix.toLocaleUpperCase("en-US"));
  }
  return false;
}
function valuesAreEqual(a, b) {
  if (a === null || b === null) {
    return false;
  }
  if (a === b) {
    return true;
  }
  if (typeof a === "string" && typeof b === "string") {
    return a.toLocaleUpperCase("en-US") === b.toLocaleUpperCase("en-US");
  }
  if (typeof a === "number" || typeof b === "number") {
    return String(a) === String(b);
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    return a.length === b.length && a.every((v, i) => valuesAreEqual(v, b[i]));
  }
  return false;
}

exports.evaluateFilterPredicate = evaluateFilterPredicate;
exports.filterPredicateToFilterFunction = filterPredicateToFilterFunction;
//# sourceMappingURL=evaluate.cjs.js.map
