'use strict';

function getJsonValueAtPath(value, path) {
  if (!path) {
    return void 0;
  }
  if (value === void 0 || value === null || typeof value !== "object" || Array.isArray(value)) {
    return void 0;
  }
  for (const valueKey in value) {
    if (!Object.hasOwn(value, valueKey)) {
      continue;
    }
    if (valueKey === path) {
      if (value[valueKey] !== void 0) {
        return value[valueKey];
      }
    }
    if (path.startsWith(`${valueKey}.`)) {
      const found = getJsonValueAtPath(
        value[valueKey],
        path.slice(valueKey.length + 1)
      );
      if (found !== void 0) {
        return found;
      }
    }
  }
  return void 0;
}

exports.getJsonValueAtPath = getJsonValueAtPath;
//# sourceMappingURL=getJsonValueAtPath.cjs.js.map
