'use strict';

const disabled = {
  refresh: false,
  unregister: false,
  move: false
};
const defaults = {
  refresh: true,
  unregister: true,
  move: true
};
function readScmEventHandlingConfig(config) {
  const rootKey = "catalog.scmEvents";
  if (!config.has(rootKey) || config.get(rootKey) === false) {
    return { ...disabled };
  } else if (config.get(rootKey) === true) {
    return { ...defaults };
  }
  return {
    refresh: config.getOptionalBoolean(`${rootKey}.refresh`) ?? defaults.refresh,
    unregister: config.getOptionalBoolean(`${rootKey}.unregister`) ?? defaults.unregister,
    move: config.getOptionalBoolean(`${rootKey}.move`) ?? defaults.move
  };
}

exports.readScmEventHandlingConfig = readScmEventHandlingConfig;
//# sourceMappingURL=readScmEventHandlingConfig.cjs.js.map
