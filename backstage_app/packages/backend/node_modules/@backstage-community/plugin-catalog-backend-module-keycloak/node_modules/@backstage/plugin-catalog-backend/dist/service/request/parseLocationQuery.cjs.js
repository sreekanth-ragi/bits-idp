'use strict';

var errors = require('@backstage/errors');
var filterPredicates = require('@backstage/filter-predicates');
var v3 = require('zod/v3');
var v3$1 = require('zod-validation-error/v3');

const filterPredicateSchema = filterPredicates.createZodV3FilterPredicateSchema(v3.z);
const locationCursorParser = v3.z.object({
  limit: v3.z.number().int().min(1),
  afterId: v3.z.string().optional(),
  query: filterPredicateSchema.optional()
});
function isSupportedFilterPredicateRoot(value) {
  if (value === void 0) {
    return true;
  }
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    return false;
  }
  return true;
}
function parseLocationQuery(request) {
  if (request.cursor !== void 0) {
    if (!request.cursor) {
      throw new errors.InputError("Cursor cannot be empty");
    }
    let parsed;
    try {
      const data = Buffer.from(request.cursor, "base64").toString("utf8");
      parsed = JSON.parse(data);
    } catch {
      throw new errors.InputError("Malformed cursor, unknown encoding");
    }
    const result = locationCursorParser.safeParse(parsed);
    if (!result.success) {
      throw new errors.InputError(`Malformed cursor: ${v3$1.fromZodError(result.error)}`);
    }
    const { query: query2, limit: limit2, afterId } = result.data;
    if (!isSupportedFilterPredicateRoot(query2)) {
      throw new errors.InputError("Query must be an object");
    }
    return {
      limit: limit2,
      afterId,
      query: query2
    };
  }
  const limit = request.limit ?? 1e3;
  if (!Number.isInteger(limit) || limit < 1) {
    throw new errors.InputError("Limit must be a positive integer >= 1");
  }
  let query;
  if (request.query !== void 0) {
    const result = filterPredicateSchema.safeParse(request.query);
    if (!result.success) {
      throw new errors.InputError(`Invalid query: ${v3$1.fromZodError(result.error)}`);
    }
    if (!isSupportedFilterPredicateRoot(result.data)) {
      throw new errors.InputError("Query must be an object");
    }
    query = result.data;
  }
  return {
    limit,
    query
  };
}
function encodeLocationQueryCursor(cursor) {
  return Buffer.from(JSON.stringify(cursor), "utf8").toString("base64");
}

exports.encodeLocationQueryCursor = encodeLocationQueryCursor;
exports.parseLocationQuery = parseLocationQuery;
//# sourceMappingURL=parseLocationQuery.cjs.js.map
