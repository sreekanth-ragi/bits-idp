'use strict';

var errors = require('@backstage/errors');
var filterPredicates = require('@backstage/filter-predicates');
var v3 = require('zod/v3');
var v3$1 = require('zod-validation-error/v3');
var util = require('../util.cjs.js');

const filterPredicateSchema = filterPredicates.createZodV3FilterPredicateSchema(v3.z);
function isSupportedFilterPredicateRoot(value) {
  if (value === void 0) {
    return true;
  }
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    return false;
  }
  return true;
}
function parseOrderFields(orderField) {
  if (!orderField?.length) {
    return void 0;
  }
  return orderField.map(({ field, order }) => {
    if (order !== "asc" && order !== "desc") {
      throw new errors.InputError("Invalid order field order, must be asc or desc");
    }
    return { field, order };
  });
}
function parseEntityQuery(request) {
  if (request.cursor !== void 0) {
    if (!request.cursor) {
      throw new errors.InputError("Cursor cannot be empty");
    }
    const cursor = util.decodeCursor(request.cursor);
    return {
      cursor,
      fields: request.fields,
      limit: request.limit
    };
  }
  let query;
  if (request.query !== void 0) {
    const result = filterPredicateSchema.safeParse(request.query);
    if (!result.success) {
      throw new errors.InputError(`Invalid query: ${v3$1.fromZodError(result.error)}`);
    }
    if (!isSupportedFilterPredicateRoot(result.data)) {
      throw new errors.InputError("Query must be an object");
    }
    query = result.data;
  }
  const orderFields = parseOrderFields(request.orderBy);
  return {
    query,
    orderFields,
    fullTextFilter: request.fullTextFilter ? {
      term: request.fullTextFilter.term ?? "",
      fields: request.fullTextFilter.fields
    } : void 0,
    fields: request.fields,
    limit: request.limit,
    offset: request.offset
  };
}

exports.parseEntityQuery = parseEntityQuery;
//# sourceMappingURL=parseEntityQuery.cjs.js.map
