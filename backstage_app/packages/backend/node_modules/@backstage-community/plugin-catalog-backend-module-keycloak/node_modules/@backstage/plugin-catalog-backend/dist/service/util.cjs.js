'use strict';

var errors = require('@backstage/errors');
var filterPredicates = require('@backstage/filter-predicates');
var lodash = require('lodash');
var v3 = require('zod/v3');
var catalogModel = require('@backstage/catalog-model');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var lodash__default = /*#__PURE__*/_interopDefaultCompat(lodash);

async function requireRequestBody(req) {
  const contentType = req.header("content-type");
  if (!contentType) {
    throw new errors.InputError("Content-Type missing");
  } else if (!contentType.match(/^application\/json($|;)/)) {
    throw new errors.InputError("Illegal Content-Type");
  }
  const body = req.body;
  if (!body) {
    throw new errors.InputError("Missing request body");
  } else if (!lodash__default.default.isPlainObject(body)) {
    throw new errors.InputError("Expected body to be a JSON object");
  } else if (Object.keys(body).length === 0) {
    throw new errors.InputError("Empty request body");
  }
  return body;
}
const locationInput = v3.z.object({
  type: v3.z.string(),
  target: v3.z.string(),
  presence: v3.z.literal("required").or(v3.z.literal("optional")).optional()
}).strict();
async function validateRequestBody(req, schema) {
  const body = await requireRequestBody(req);
  try {
    return await schema.parse(body);
  } catch (e) {
    throw new errors.InputError(`Malformed request: ${e}`);
  }
}
function disallowReadonlyMode(readonly) {
  if (readonly) {
    throw new errors.NotAllowedError("This operation not allowed in readonly mode");
  }
}
function isQueryEntitiesInitialRequest(input) {
  if (!input) {
    return false;
  }
  return !isQueryEntitiesCursorRequest(input);
}
function isQueryEntitiesCursorRequest(input) {
  if (!input) {
    return false;
  }
  return !!input.cursor;
}
const entityFilterParser = v3.z.lazy(
  () => v3.z.object({
    key: v3.z.string(),
    values: v3.z.array(v3.z.string()).optional()
  }).or(v3.z.object({ not: entityFilterParser })).or(v3.z.object({ anyOf: v3.z.array(entityFilterParser) })).or(v3.z.object({ allOf: v3.z.array(entityFilterParser) }))
);
const filterPredicateSchema = filterPredicates.createZodV3FilterPredicateSchema(v3.z);
const cursorParser = v3.z.object({
  orderFields: v3.z.array(
    v3.z.object({ field: v3.z.string(), order: v3.z.enum(["asc", "desc"]) })
  ),
  fullTextFilter: v3.z.object({
    term: v3.z.string(),
    fields: v3.z.array(v3.z.string()).optional()
  }).optional(),
  orderFieldValues: v3.z.array(v3.z.string().or(v3.z.null())),
  filter: entityFilterParser.optional(),
  isPrevious: v3.z.boolean(),
  query: filterPredicateSchema.optional(),
  firstSortFieldValues: v3.z.array(v3.z.string().or(v3.z.null())).optional(),
  totalItems: v3.z.number().optional()
});
function encodeCursor(cursor) {
  const json = JSON.stringify(cursor);
  return Buffer.from(json, "utf8").toString("base64");
}
function decodeCursor(encodedCursor) {
  try {
    const data = Buffer.from(encodedCursor, "base64").toString("utf8");
    const result = cursorParser.safeParse(JSON.parse(data));
    if (!result.success) {
      throw new errors.InputError(`Malformed cursor: ${result.error}`);
    }
    return result.data;
  } catch (e) {
    throw new errors.InputError(`Malformed cursor: ${e}`);
  }
}
function expandLegacyCompoundRelationsInEntity(entity) {
  if (entity.relations) {
    for (const relation of entity.relations) {
      if (!relation.targetRef && relation.target) {
        relation.targetRef = catalogModel.stringifyEntityRef(relation.target);
      } else if (!relation.target && relation.targetRef) {
        relation.target = catalogModel.parseEntityRef(relation.targetRef);
      }
    }
  }
  return entity;
}
function filterAndSortProcessors(processors, config) {
  function getProcessorOptions(processor) {
    const root = config.getOptionalConfig("catalog.processorOptions");
    try {
      return root?.getOptionalConfig(processor.getProcessorName());
    } catch {
      return void 0;
    }
  }
  function isProcessorDisabled(processor) {
    return getProcessorOptions(processor)?.getOptionalBoolean("disabled") === true;
  }
  function getProcessorPriority(processor) {
    let priority = getProcessorOptions(processor)?.getOptionalNumber("priority");
    if (priority === void 0) {
      try {
        priority = processor.getPriority?.();
      } catch {
      }
    }
    return priority ?? 20;
  }
  return processors.filter((p) => !isProcessorDisabled(p)).sort((a, b) => getProcessorPriority(a) - getProcessorPriority(b));
}
function filterProviders(providers, config) {
  function getProviderOptions(entry) {
    const root = config.getOptionalConfig("catalog.providerOptions");
    try {
      return root?.getOptionalConfig(entry.provider.getProviderName());
    } catch {
      return void 0;
    }
  }
  function isProviderDisabled(entry) {
    return getProviderOptions(entry)?.getOptionalBoolean("disabled") === true;
  }
  return providers.filter((entry) => !isProviderDisabled(entry));
}

exports.cursorParser = cursorParser;
exports.decodeCursor = decodeCursor;
exports.disallowReadonlyMode = disallowReadonlyMode;
exports.encodeCursor = encodeCursor;
exports.expandLegacyCompoundRelationsInEntity = expandLegacyCompoundRelationsInEntity;
exports.filterAndSortProcessors = filterAndSortProcessors;
exports.filterProviders = filterProviders;
exports.isQueryEntitiesCursorRequest = isQueryEntitiesCursorRequest;
exports.isQueryEntitiesInitialRequest = isQueryEntitiesInitialRequest;
exports.locationInput = locationInput;
exports.requireRequestBody = requireRequestBody;
exports.validateRequestBody = validateRequestBody;
//# sourceMappingURL=util.cjs.js.map
